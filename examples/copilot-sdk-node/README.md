# Copilot SDK example for TRAE-CLI

This example demonstrates creating a Copilot SDK session and sending a prompt.

Usage (locally):

1. Install deps: `npm install` 
2. Ensure Copilot credentials are available (e.g. `COPILOT_GITHUB_TOKEN` or `copilot` logged in).
3. Run: `node index.js`